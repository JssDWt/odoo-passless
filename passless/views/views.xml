<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="passless.list">
      <field name="name">passless list</field>
      <field name="model">passless.passless</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="passless.action_window">
      <field name="name">passless window</field>
      <field name="res_model">passless.passless</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="passless.action_server">
      <field name="name">passless server</field>
      <field name="model_id" ref="model_passless_passless"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="passless" id="passless.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="passless.menu_1" parent="passless.menu_root"/>
    <menuitem name="Menu 2" id="passless.menu_2" parent="passless.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="passless.menu_1_list" parent="passless.menu_1"
              action="passless.action_window"/>
    <menuitem name="Server to list" id="passless" parent="passless.menu_2"
              action="passless.action_server"/>
    -->
  </data>
</odoo>